---
phase: 02-public-portfolio
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/projects-section.tsx
  - src/components/projects/project-card.tsx
  - src/components/projects/project-filter.tsx
  - src/components/projects/project-gallery.tsx
  - src/app/(public)/projects/[slug]/page.tsx
autonomous: true
requirements: [PROJ-01, PROJ-02, PROJ-05, DSGN-03]

must_haves:
  truths:
    - "Visitor sees projects in a grid with image-first cards showing title, description, tech tags, and links"
    - "Visitor can filter projects by clicking tech stack tag buttons above the grid"
    - "Clicking a project card navigates to /projects/[slug] detail page"
    - "Project detail page shows hero image, thumbnail gallery, full description, tech stack, and GitHub/demo links"
    - "Gallery swaps main image when thumbnail is clicked"
    - "All layouts are responsive across mobile and desktop"
  artifacts:
    - path: "src/components/sections/projects-section.tsx"
      provides: "Server component fetching projects and passing to filter"
      exports: ["ProjectsSection"]
    - path: "src/components/projects/project-card.tsx"
      provides: "Individual project card component"
      exports: ["ProjectCard"]
    - path: "src/components/projects/project-filter.tsx"
      provides: "Client component for tech stack filtering"
      exports: ["ProjectFilter"]
    - path: "src/components/projects/project-gallery.tsx"
      provides: "Client component for image gallery with thumbnail swap"
      exports: ["ProjectGallery"]
    - path: "src/app/(public)/projects/[slug]/page.tsx"
      provides: "Project detail page with full content and gallery"
  key_links:
    - from: "src/components/sections/projects-section.tsx"
      to: "src/lib/queries/projects.ts"
      via: "getProjects server fetch"
      pattern: "getProjects"
    - from: "src/components/sections/projects-section.tsx"
      to: "src/components/projects/project-filter.tsx"
      via: "passes projects as props to client component"
      pattern: "ProjectFilter.*projects"
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/lib/queries/projects.ts"
      via: "getProjectBySlug server fetch"
      pattern: "getProjectBySlug"
    - from: "src/app/(public)/projects/[slug]/page.tsx"
      to: "src/components/projects/project-gallery.tsx"
      via: "passes images to gallery client component"
      pattern: "ProjectGallery"
---

<objective>
Build the Projects section with filterable grid and individual project detail pages with image galleries.

Purpose: Projects are the most complex public-facing feature, requiring client-side filtering, dynamic routing, and an interactive image gallery. Separating this from the simpler sections allows focused implementation.
Output: Projects section component, project card, filter client component, gallery client component, and /projects/[slug] detail page.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-portfolio/02-CONTEXT.md
@.planning/phases/02-public-portfolio/02-RESEARCH.md
@.planning/phases/02-public-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - types and queries -->
From src/lib/types/database.ts:
```typescript
export interface Project {
  id: string; title: string; slug: string; short_description: string | null;
  full_description: string | null; tech_stack: string[]; github_url: string | null;
  live_url: string | null; thumbnail_url: string | null; featured: boolean;
  display_order: number;
}
export interface ProjectImage {
  id: string; project_id: string; image_url: string; alt_text: string | null;
  display_order: number;
}
```

From src/lib/queries/projects.ts:
```typescript
export async function getProjects(): Promise<Project[]>
export async function getProjectBySlug(slug: string): Promise<(Project & { images: ProjectImage[] }) | null>
```

<!-- Existing UI components -->
From src/components/ui/: Button, Card (CardHeader, CardTitle, CardDescription, CardContent, CardFooter), Badge
From src/lib/utils.ts: cn()
From next/image: Image (configured for Supabase remote patterns)
From next/link: Link
From lucide-react: ExternalLink, Github, ArrowLeft, etc.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project card, filter, and projects section</name>
  <files>
    src/components/projects/project-card.tsx
    src/components/projects/project-filter.tsx
    src/components/sections/projects-section.tsx
  </files>
  <action>
Create `src/components/projects/project-card.tsx` (server component, no 'use client'):
- Props: `project: Project` (import type from `@/lib/types/database`)
- **Layout (per user decision):** Image-first card.
  - Thumbnail image at top using Next.js Image, aspect-video, object-cover, rounded-t-lg. If no thumbnail_url, show a gradient placeholder div.
  - Below image: title (CardTitle as h3), short_description (text-sm text-muted-foreground, line-clamp-2).
  - Tech tags: flex flex-wrap gap-1 of Badge components (variant="secondary", text-xs) for each item in tech_stack.
  - Footer with links: GitHub icon link and External link icon for live_url. Only render each if URL exists.
  - Entire card wrapped in `<Link href={/projects/${project.slug}}>` for navigation.
  - **Per user specifics:** Add hover effect -- group class on card, group-hover:shadow-lg transition-all, slight scale on hover (group-hover:scale-[1.02]).

Create `src/components/projects/project-filter.tsx` ('use client'):
- Props: `projects: Project[]`
- State: `activeFilter` string, default "All".
- Extract unique tech tags: `['All', ...new Set(projects.flatMap(p => p.tech_stack))]`.
- Filter buttons: flex flex-wrap gap-2 of buttons above the grid. Active button uses Button variant="default", inactive uses variant="outline". Each calls `setActiveFilter(tech)`.
- Filtered projects: if "All", show all; otherwise filter by `tech_stack.includes(activeFilter)`.
- Render filtered projects in a grid: `grid gap-6 sm:grid-cols-2 lg:grid-cols-3`.
- Each project renders `<ProjectCard project={project} />`.
- If no projects match filter, show a subtle "No projects found" message.

Create `src/components/sections/projects-section.tsx` (async server component):
- Import `getProjects` from `@/lib/queries/projects`.
- Fetch projects. If empty array, return null.
- Section wrapper: `<section id="projects" className="py-16 md:py-24">` with container.
- Title: "Projects" as h2 (text-3xl font-bold tracking-tight mb-8 md:mb-12).
- Render `<ProjectFilter projects={projects} />` passing the server-fetched data.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects/portfolio-with-gsd" && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ProjectsSection fetches projects server-side and passes to ProjectFilter. ProjectFilter renders clickable tag buttons and a grid of ProjectCards. Cards have image-first layout with hover effects. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Project detail page and image gallery</name>
  <files>
    src/components/projects/project-gallery.tsx
    src/app/(public)/projects/[slug]/page.tsx
  </files>
  <action>
Create `src/components/projects/project-gallery.tsx` ('use client'):
- Props: `images: ProjectImage[]`, `projectTitle: string`
- State: `selectedIndex` number, default 0.
- **Layout (per user decision):** Hero image with thumbnail gallery below that swaps main image on click.
  - Main image: large Image component, aspect-video, object-cover, rounded-lg. Shows `images[selectedIndex]`. Alt text from image's alt_text field or `${projectTitle} screenshot ${index + 1}`.
  - Thumbnail strip below: flex gap-2, overflow-x-auto for horizontal scrolling on mobile.
  - Each thumbnail: Image with width={120} height={80}, object-cover, rounded-md, cursor-pointer.
  - Active thumbnail: ring-2 ring-primary. Others: opacity-70 hover:opacity-100 transition-opacity.
  - Clicking a thumbnail sets `setSelectedIndex(index)`.
- If only 1 image or no images, just show the single image without thumbnail strip.

Create `src/app/(public)/projects/[slug]/page.tsx` (async server component):
- Import `getProjectBySlug` from `@/lib/queries/projects`.
- **CRITICAL: Next.js 16 async params.** Type params as `Promise<{ slug: string }>` and await it: `const { slug } = await params`.
- Fetch project by slug. If null, call `notFound()` from `next/navigation`.
- **Layout:**
  - Back button at top: `<Link href="/#projects">` with ArrowLeft icon and "Back to Projects" text.
  - Title as h1 (text-3xl md:text-4xl font-bold).
  - If project has images (images.length > 0), render `<ProjectGallery images={project.images} projectTitle={project.title} />`.
  - Else if thumbnail_url exists, show single Image.
  - Below gallery: two-column layout on md+ (md:grid md:grid-cols-3 gap-8).
    - Left column (md:col-span-2): full_description rendered as paragraphs (split on `\n\n`). Use `prose` or manual paragraph styling with `text-muted-foreground leading-relaxed`.
    - Right sidebar (md:col-span-1): Card with tech stack (Badge list), links section with GitHub button and Live Demo button (only if URLs exist, using ExternalLink and Github lucide icons, Button variant="outline" with full width).
  - Responsive: On mobile, sidebar stacks below description.
- Export `generateMetadata` for SEO: title from project.title, description from short_description.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects/portfolio-with-gsd" && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <done>Project detail page at /projects/[slug] renders with gallery, full description, tech stack sidebar, and links. Gallery swaps main image on thumbnail click. 404 for invalid slugs. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm run build` passes with zero errors
- ProjectsSection exports async server component
- ProjectFilter is a 'use client' component with filter state
- ProjectCard has image-first layout with hover effects
- ProjectGallery swaps main image on thumbnail click
- /projects/[slug] page uses async params pattern
- All components handle empty/null data gracefully
</verification>

<success_criteria>
- Projects grid renders with image-first cards and tech tag badges
- Filter buttons extract unique techs and filter the grid
- Clicking a card navigates to /projects/[slug]
- Detail page shows gallery, description, tech stack, and links
- Gallery thumbnail click swaps the hero image
- Responsive across mobile and desktop
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio/02-03-SUMMARY.md`
</output>
