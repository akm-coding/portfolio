---
phase: 02-public-portfolio
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/components/sections/hero-section.tsx
  - src/components/sections/about-section.tsx
  - src/components/sections/experience-section.tsx
  - src/components/sections/education-section.tsx
  - src/components/sections/skills-section.tsx
autonomous: true
requirements: [PROF-01, PROF-02, PROF-03, EXPR-01, EDUC-01, SKLL-01, DSGN-03]

must_haves:
  truths:
    - "Hero section shows name, title, tagline, profile photo, Resume download button, and Contact scroll button"
    - "About section shows bio text with resume download button below"
    - "Experience section shows vertical timeline with alternating left/right cards"
    - "Education section shows stacked cards with institution, degree, field, dates"
    - "Skills section shows grouped tag badges by category with colored pills"
    - "All sections are responsive across mobile, tablet, and desktop"
    - "Empty states are handled gracefully when no data exists"
  artifacts:
    - path: "src/components/sections/hero-section.tsx"
      provides: "Hero with split layout, photo, CTA buttons"
      exports: ["HeroSection"]
    - path: "src/components/sections/about-section.tsx"
      provides: "About prose block with resume link"
      exports: ["AboutSection"]
    - path: "src/components/sections/experience-section.tsx"
      provides: "Timeline layout for work experience"
      exports: ["ExperienceSection"]
    - path: "src/components/sections/education-section.tsx"
      provides: "Card layout for education entries"
      exports: ["EducationSection"]
    - path: "src/components/sections/skills-section.tsx"
      provides: "Grouped badge layout for skills"
      exports: ["SkillsSection"]
  key_links:
    - from: "src/components/sections/hero-section.tsx"
      to: "src/lib/queries/profile.ts"
      via: "server component data fetch"
      pattern: "getProfile"
    - from: "src/components/sections/experience-section.tsx"
      to: "src/lib/queries/experiences.ts"
      via: "server component data fetch"
      pattern: "getExperiences"
    - from: "src/components/sections/skills-section.tsx"
      to: "src/lib/queries/skills.ts"
      via: "server component data fetch"
      pattern: "getSkills"
---

<objective>
Build the Hero, About, Experience, Education, and Skills portfolio sections as server components fetching real data from Supabase.

Purpose: These five sections make up the majority of the single-page portfolio. They are all server components with no client-side interactivity, making them a natural batch.
Output: Five section components ready to be composed into the home page (Plan 04 handles composition).
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-public-portfolio/02-CONTEXT.md
@.planning/phases/02-public-portfolio/02-RESEARCH.md
@.planning/phases/02-public-portfolio/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - types and queries this plan depends on -->
From src/lib/types/database.ts:
```typescript
export interface Profile {
  id: string; full_name: string; title: string; tagline: string | null;
  bio: string | null; avatar_url: string | null; resume_url: string | null;
  email: string | null; phone: string | null; location: string | null;
  github_url: string | null; linkedin_url: string | null;
}
export interface Experience {
  id: string; company: string; role: string; description: string | null;
  start_date: string; end_date: string | null; is_current: boolean; display_order: number;
}
export interface Education {
  id: string; institution: string; degree: string; field_of_study: string | null;
  start_date: string; end_date: string | null; description: string | null; display_order: number;
}
export interface Skill {
  id: string; name: string; category: string; display_order: number;
}
```

From src/lib/queries/profile.ts:
```typescript
export async function getProfile(): Promise<Profile | null>
```

From src/lib/queries/experiences.ts:
```typescript
export async function getExperiences(): Promise<Experience[]>
```

From src/lib/queries/education.ts:
```typescript
export async function getEducation(): Promise<Education[]>
```

From src/lib/queries/skills.ts:
```typescript
export async function getSkills(): Promise<Skill[]>
```

<!-- Existing UI components available -->
From src/components/ui/: Button, Card (CardHeader, CardTitle, CardDescription, CardContent), Badge, Avatar, Input, Label
From src/lib/utils.ts: cn() for className merging
From next/image: Image component (configured for Supabase remote patterns)
From lucide-react: All icons available (Download, Mail, ArrowDown, MapPin, Calendar, Briefcase, GraduationCap, etc.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero and About sections</name>
  <files>
    src/components/sections/hero-section.tsx
    src/components/sections/about-section.tsx
  </files>
  <action>
Create `src/components/sections/hero-section.tsx` as an async server component:
- Import `getProfile` from `@/lib/queries/profile`
- Fetch profile data. If null, render nothing (empty state guard).
- **Layout (per user decision):** Split layout -- text on the left, profile photo on the right.
  - Left side: `full_name` as large heading (text-4xl md:text-5xl lg:text-6xl font-bold), `title` as subheading (text-xl md:text-2xl text-muted-foreground), `tagline` as paragraph below.
  - Two CTA buttons below text:
    - "Download Resume" button (primary variant) linking to `resume_url` with download attribute and Download icon from lucide-react. Only render if `resume_url` exists.
    - "Contact Me" button (outline variant) as an anchor `href="#contact"` with Mail icon.
  - Right side: Profile photo using Next.js `<Image>` component with `avatar_url`, rounded-lg (rounded square per user decision, NOT circular), aspect-square, object-cover. Use width={400} height={400}. Show a placeholder div with initials if no avatar_url.
- **Responsive (per user decision):** On mobile (default), stack vertically. On md+, use grid grid-cols-2 with items-center gap-8.
- Use `<section id="hero" className="min-h-[calc(100vh-3.5rem)] flex items-center">` for full viewport height minus navbar.
- Wrap content in `<div className="container mx-auto px-4">`.

Create `src/components/sections/about-section.tsx` as an async server component:
- Import `getProfile` from `@/lib/queries/profile`
- Fetch profile. If null or no bio, render nothing.
- **Layout (per user decision):** Simple prose block -- clean paragraph text.
  - Section title: "About Me" as h2 (text-3xl font-bold tracking-tight mb-8).
  - Bio rendered as paragraphs (split on `\n\n` if multi-paragraph). Use `text-lg text-muted-foreground leading-relaxed max-w-3xl`.
  - Resume download button below bio (Button variant outline with Download icon). Only show if `resume_url` exists.
- Use `<section id="about" className="py-16 md:py-24">` with container wrapper.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects/portfolio-with-gsd" && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <done>HeroSection renders split layout with name/title/tagline/photo/CTAs. AboutSection renders bio prose with resume link. Both handle null profile gracefully. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Experience, Education, and Skills sections</name>
  <files>
    src/components/sections/experience-section.tsx
    src/components/sections/education-section.tsx
    src/components/sections/skills-section.tsx
  </files>
  <action>
Create `src/components/sections/experience-section.tsx` as an async server component:
- Import `getExperiences` from `@/lib/queries/experiences`
- Fetch experiences. If empty array, render nothing (return null).
- **Layout (per user decision):** Vertical timeline with alternating left/right cards.
  - Section title: "Experience" as h2 (text-3xl font-bold tracking-tight mb-8 md:mb-12).
  - Timeline container: `relative` div.
  - Vertical line: `absolute left-1/2 top-0 bottom-0 w-px bg-border hidden md:block` (only visible on md+).
  - Each entry is a Card positioned alternating sides on md+ (even index: md:pr-12 md:ml-0 md:w-1/2, odd index: md:pl-12 md:ml-auto md:w-1/2).
  - Timeline dot: small circle (w-3 h-3 rounded-full bg-primary) positioned at the center line on md+.
  - Card content: company name (CardTitle, with Briefcase icon), role (font-semibold text-primary), date range formatted as "MMM YYYY - MMM YYYY" or "MMM YYYY - Present" if is_current, description paragraph.
  - **Mobile:** Stack all cards vertically with left border instead (border-l-2 border-primary pl-4 ml-2) -- no alternating, no center line.
  - Use Calendar icon from lucide-react for dates.

Create `src/components/sections/education-section.tsx` as an async server component:
- Import `getEducation` from `@/lib/queries/education`
- Fetch education entries. If empty, return null.
- **Layout (per user decision):** Simple stacked cards (NOT timeline).
  - Section title: "Education" as h2.
  - Grid layout: `grid gap-6 md:grid-cols-2` for two columns on desktop.
  - Each entry is a Card with: institution name (CardTitle, with GraduationCap icon), degree and field_of_study (e.g., "Bachelor of Science in Computer Science"), date range, description if present.
  - Subtle card styling: hover:shadow-md transition-shadow.

Create `src/components/sections/skills-section.tsx` as an async server component:
- Import `getSkills` from `@/lib/queries/skills`
- Fetch skills. If empty, return null.
- **Layout (per user decision):** Grouped tag badges by category. No progress bars.
  - Section title: "Skills" as h2.
  - Group skills by `category` field (use `Object.groupBy` or reduce to Map).
  - For each category: category name as h3 (text-lg font-semibold mb-3), then flex flex-wrap gap-2 of Badge components.
  - **Per user specifics:** Visually distinguish categories with different accent colors. Use a map of category -> variant/color: e.g., "Frontend" -> default badge, "Backend" -> secondary badge, "Tools & DevOps" -> outline badge. Use Shadcn Badge component. Add additional categories as needed with cycling colors.
  - Grid the category groups: `grid gap-8 md:grid-cols-2 lg:grid-cols-3`.

All three sections use `<section id="{name}" className="py-16 md:py-24">` with container mx-auto px-4 wrapper.

Format dates using a helper: create a small `formatDate` helper inline or as a utility that converts "YYYY-MM-DD" to "MMM YYYY" using `new Date(date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' })`.
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects/portfolio-with-gsd" && pnpm run build 2>&1 | tail -5</automated>
  </verify>
  <done>ExperienceSection renders alternating timeline on desktop and stacked cards on mobile. EducationSection renders card grid. SkillsSection renders grouped badges by category. All handle empty arrays gracefully. Build passes.</done>
</task>

</tasks>

<verification>
- `pnpm run build` passes with zero errors
- All 5 section components export async server component functions
- Each section has correct `id` attribute for anchor navigation
- Each section handles null/empty data without crashing
- Hero has split layout with photo and CTA buttons
- Experience has alternating timeline on desktop, stacked on mobile
- Skills use Badge component with category grouping
</verification>

<success_criteria>
- 5 section components created in `src/components/sections/`
- All components fetch real data from Supabase via query functions
- Responsive layouts per user decisions (split hero, alternating timeline, card grid, badge groups)
- Empty state handling (return null when no data)
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/02-public-portfolio/02-02-SUMMARY.md`
</output>
