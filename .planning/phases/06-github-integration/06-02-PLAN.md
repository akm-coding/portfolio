---
phase: 06-github-integration
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/components/github/contribution-heatmap.tsx
  - src/components/sections/github-section.tsx
  - src/app/(public)/page.tsx
autonomous: false
requirements: [GH-03, GH-04]

must_haves:
  truths:
    - "Contribution heatmap displays a full year of GitHub activity with green color scheme"
    - "Heatmap colors adapt to dark/light theme while staying green-based"
    - "Tooltips show 'X contributions on Mon DD, YYYY' on hover"
    - "GitHub section appears after Statistics and before Projects on home page"
    - "Section layout: pinned repos grid on top, stats row in middle, heatmap at bottom"
    - "Section heading is 'My GitHub Activity' with no subtitle"
  artifacts:
    - path: "src/components/github/contribution-heatmap.tsx"
      provides: "Client component wrapping react-activity-calendar with theme support"
      exports: ["ContributionHeatmap"]
    - path: "src/components/sections/github-section.tsx"
      provides: "Server component composing all GitHub sub-components"
      exports: ["GitHubSection"]
    - path: "src/app/(public)/page.tsx"
      provides: "Home page with GitHubSection inserted after StatisticsSection"
      contains: "GitHubSection"
  key_links:
    - from: "src/components/sections/github-section.tsx"
      to: "src/lib/queries/github.ts"
      via: "imports getGitHubData"
      pattern: "import.*getGitHubData.*from.*queries/github"
    - from: "src/components/sections/github-section.tsx"
      to: "src/components/github/repo-card.tsx"
      via: "renders RepoCard for each pinned repo"
      pattern: "RepoCard"
    - from: "src/components/github/contribution-heatmap.tsx"
      to: "react-activity-calendar"
      via: "ActivityCalendar component with theme prop"
      pattern: "import ActivityCalendar"
    - from: "src/app/(public)/page.tsx"
      to: "src/components/sections/github-section.tsx"
      via: "renders GitHubSection between Statistics and Projects"
      pattern: "GitHubSection"
---

<objective>
Create the contribution heatmap component, compose the full GitHub section (repos + stats + heatmap), and wire it into the home page between Statistics and Projects.

Purpose: Completes the public-facing GitHub integration section with all visual elements composed and placed correctly.
Output: Fully functional GitHub section on the home page showing pinned repos, stats, and contribution heatmap.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-github-integration/06-CONTEXT.md
@.planning/phases/06-github-integration/06-RESEARCH.md
@.planning/phases/06-github-integration/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs -->

From src/lib/types/database.ts (new types):
```typescript
export interface PinnedRepo {
  name: string
  nameWithOwner: string
  description: string | null
  url: string
  stargazerCount: number
  forkCount: number
  primaryLanguage: { name: string; color: string } | null
}

export interface GitHubData {
  pinnedRepos: PinnedRepo[]
  totalContributions: number
  totalRepos: number
  totalStars: number
  contributionDays: Array<{ date: string; count: number; level: 0 | 1 | 2 | 3 | 4 }>
}
```

From src/lib/queries/github.ts:
```typescript
export async function getGitHubData(): Promise<GitHubData | null>
```

From src/components/github/repo-card.tsx:
```typescript
export function RepoCard({ repo }: { repo: PinnedRepo })
```

From src/components/github/github-stats-row.tsx:
```typescript
export function GitHubStatsRow({ totalContributions, totalRepos, totalStars }: {...})
```

From src/app/(public)/page.tsx (current order):
```typescript
<HeroSection />
<ScrollReveal><AboutSection /></ScrollReveal>
<StatisticsSection />
<ScrollReveal><ProjectsSection /></ScrollReveal>
// ... rest of sections
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ContributionHeatmap component and GitHubSection server component</name>
  <files>
    src/components/github/contribution-heatmap.tsx
    src/components/sections/github-section.tsx
    src/app/(public)/page.tsx
  </files>
  <action>
1. Create `src/components/github/contribution-heatmap.tsx` ('use client'):
   - Import ActivityCalendar from 'react-activity-calendar', useTheme from 'next-themes', useState/useEffect from 'react'
   - Props: `{ activities: Array<{ date: string; count: number; level: 0|1|2|3|4 }> }`
   - Define green theme object: light: ['#ebedf0', '#9be9a8', '#40c463', '#30a14e', '#216e39'], dark: ['#161b22', '#0e4429', '#006d32', '#26a641', '#39d353']
   - Use mounted state pattern to avoid hydration mismatch (same approach as DynamicIcon fix from commit fad2323): `const [mounted, setMounted] = useState(false)` + `useEffect(() => setMounted(true), [])` + render skeleton/null until mounted
   - Render ActivityCalendar with: data={activities}, theme={greenTheme}, colorScheme={resolvedTheme as 'light' | 'dark'}, labels={{ totalCount: '{{count}} contributions in the last year' }}
   - For tooltips: use `renderBlock` prop to wrap each block with a native HTML `title` attribute via cloneElement or a wrapping `<g>` element: format "X contributions on Mon DD, YYYY" using `new Date(activity.date).toLocaleDateString('en-US', { month: 'short', day: 'numeric', year: 'numeric' })`. If react-activity-calendar v3 supports `renderBlock`, use it. Otherwise, check if there's a tooltip prop. The research says renderBlock works -- use it to wrap each block in a Tooltip-like element. Since Shadcn Tooltip is not installed, use a simple approach: either native `title` on a wrapping `<rect>` or install and use the Shadcn tooltip (`npx shadcn@latest add tooltip --yes` if the renderBlock approach needs proper floating tooltips).
   - Set hideColorLegend={false}, hideTotalCount={true} (we show our own stats row), showWeekdayLabels={false} (per user decision: no day labels on side)

2. Create `src/components/sections/github-section.tsx` (server component, NO 'use client'):
   - Import getGitHubData from '@/lib/queries/github', RepoCard from '@/components/github/repo-card', GitHubStatsRow from '@/components/github/github-stats-row', ContributionHeatmap from '@/components/github/contribution-heatmap', ScrollReveal from '@/components/motion/scroll-reveal'
   - Export async function GitHubSection()
   - Call `const data = await getGitHubData()`; if null, return null (graceful empty state)
   - Render section with py-16 px-4, max-w-6xl mx-auto container
   - Section heading: `<h2 className="text-3xl font-bold tracking-tight mb-8">My GitHub Activity</h2>` -- heading only, NO subtitle (per user decision)
   - Layout order (per user decision):
     a. Pinned repos grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4` -- map data.pinnedRepos to RepoCard components. Only render grid if pinnedRepos.length > 0.
     b. Stats row: `<GitHubStatsRow totalContributions={data.totalContributions} totalRepos={data.totalRepos} totalStars={data.totalStars} />`
     c. Heatmap: `<ContributionHeatmap activities={data.contributionDays} />`
   - Wrap the entire section content in ScrollReveal for consistent animation with other sections

3. Wire into home page `src/app/(public)/page.tsx`:
   - Import GitHubSection from '@/components/sections/github-section'
   - Place `<GitHubSection />` AFTER `<StatisticsSection />` and BEFORE `<ScrollReveal><ProjectsSection /></ScrollReveal>` (per user decision: after Statistics, before Projects)
   - Do NOT wrap GitHubSection in ScrollReveal here since it handles its own ScrollReveal internally
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects/portfolio-with-gsd" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>GitHub section renders on home page with pinned repos grid, animated stats row, and contribution heatmap. Heatmap adapts colors to dark/light theme. Section appears after Statistics and before Projects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify GitHub section visual quality and interactivity</name>
  <files>src/app/(public)/page.tsx</files>
  <action>Human visually verifies the complete GitHub integration section on the home page. No code changes -- this is a visual and functional quality gate.</action>
  <verify>Human confirms all 10 verification steps pass</verify>
  <done>GitHub section approved: repos display correctly, stats animate, heatmap renders with theme support, tooltips work, responsive layout confirmed</done>
  <what-built>Complete GitHub integration section on the home page: pinned repos grid (3x2 on desktop), animated stats summary row with count-up, and full-year contribution heatmap with green theme that adapts to dark/light mode.</what-built>
  <how-to-verify>
    1. Run `npm run dev` and visit http://localhost:3000
    2. Scroll down past Statistics section -- you should see "My GitHub Activity" heading
    3. Verify pinned repos display as cards with: owner/name heading, description, colored language dot, star count, fork count, and external link icon in corner
    4. Click an external link icon -- should open the repo on GitHub in a new tab
    5. Verify stats row shows "X contributions · Y repos · Z stars" with numbers animating up when scrolled into view
    6. Verify contribution heatmap shows a full year of green squares with month labels at top
    7. Hover over heatmap cells -- should see tooltip with "X contributions on Mon DD, YYYY"
    8. Toggle dark/light theme -- heatmap colors should adapt (darker background in dark mode, lighter in light mode) while staying green-based
    9. Resize browser to mobile width -- repos should stack to single column, heatmap should scale
    10. Note: If GITHUB_TOKEN env var is not set, the section will not appear (graceful degradation). Set GITHUB_TOKEN in .env.local if needed.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` succeeds with no errors
- Home page renders GitHubSection between Statistics and Projects
- Heatmap renders with react-activity-calendar and adapts to theme
- Stats row animates on scroll
- Repo cards show all required fields with external link
- Section gracefully handles missing GITHUB_TOKEN (returns null, section hidden)
</verification>

<success_criteria>
- Contribution heatmap displays full year with GitHub green color scheme
- Heatmap colors adapt to dark/light theme
- Tooltips show contribution count and formatted date on hover
- Section is placed correctly between Statistics and Projects
- Layout follows user decision: repos on top, stats row in middle, heatmap at bottom
- Human verification confirms visual quality and interactivity
</success_criteria>

<output>
After completion, create `.planning/phases/06-github-integration/06-02-SUMMARY.md`
</output>
