---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/theme/theme-provider.tsx
  - src/components/theme/theme-toggle.tsx
  - src/app/layout.tsx
  - src/components/layout/public-navbar.tsx
  - src/components/layout/mobile-nav.tsx
  - src/components/layout/admin-sidebar.tsx
  - src/app/admin/layout.tsx
  - src/app/(public)/layout.tsx
autonomous: false
requirements:
  - DSGN-01

must_haves:
  truths:
    - "Theme toggle cycles through light, dark, and system modes via icon click"
    - "Theme preference persists across page refresh (localStorage)"
    - "No flash of unstyled/wrong-theme content on initial page load"
    - "Admin dashboard is always dark mode regardless of public theme setting"
    - "Public navbar is sticky, visible while scrolling, responsive on all screen sizes"
    - "Mobile navigation shows hamburger that opens a slide-out menu"
    - "Admin sidebar collapses to a Sheet on mobile"
  artifacts:
    - path: "src/components/theme/theme-provider.tsx"
      provides: "next-themes ThemeProvider wrapper"
      exports: ["ThemeProvider"]
    - path: "src/components/theme/theme-toggle.tsx"
      provides: "Three-state theme toggle (Sun/Moon/Monitor icons)"
      exports: ["ThemeToggle"]
    - path: "src/components/layout/public-navbar.tsx"
      provides: "Sticky public navbar with nav links and theme toggle"
      exports: ["PublicNavbar"]
    - path: "src/components/layout/admin-sidebar.tsx"
      provides: "Admin sidebar with dark mode force and Sheet on mobile"
      exports: ["AdminSidebar"]
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/components/theme/theme-provider.tsx"
      via: "ThemeProvider wraps entire app"
      pattern: "ThemeProvider"
    - from: "src/components/layout/public-navbar.tsx"
      to: "src/components/theme/theme-toggle.tsx"
      via: "ThemeToggle rendered in navbar top-right"
      pattern: "ThemeToggle"
    - from: "src/app/admin/layout.tsx"
      to: "src/components/layout/admin-sidebar.tsx"
      via: "AdminSidebar rendered in admin layout with forced dark class"
      pattern: "AdminSidebar"
---

<objective>
Implement the dark/light/system theme system and build the responsive navigation shell for both public pages and the admin dashboard.

Purpose: Establish the visual framework and navigation patterns that all content pages in Phase 2 and 3 will render within.
Output: Theme toggle working across the site, public sticky navbar with section links, admin sidebar layout with forced dark mode.
</objective>

<execution_context>
@/Users/aungkaungmyat/.claude/get-shit-done/workflows/execute-plan.md
@/Users/aungkaungmyat/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Theme system with three-state toggle and admin dark-mode override</name>
  <files>
    src/components/theme/theme-provider.tsx
    src/components/theme/theme-toggle.tsx
    src/app/layout.tsx
  </files>
  <action>
1. Install next-themes: `npm install next-themes`

2. Install lucide-react for icons (if not already from Shadcn): `npm install lucide-react`

3. **src/components/theme/theme-provider.tsx** — Client component ("use client"):
   - Wraps `next-themes` ThemeProvider with `attribute="class"`, `defaultTheme="system"`, `enableSystem`, `disableTransitionOnChange` (prevents flash)
   - Export as `ThemeProvider`

4. **src/components/theme/theme-toggle.tsx** — Client component ("use client"):
   - Uses `useTheme()` from next-themes
   - Renders a single button that cycles through: light -> dark -> system on each click
   - Shows icon based on current resolved state:
     - When theme is "light": Sun icon
     - When theme is "dark": Moon icon
     - When theme is "system": Monitor icon
   - Use Shadcn Button variant="ghost" size="icon"
   - Icons from lucide-react: Sun, Moon, Monitor
   - Handle the mounted state to avoid hydration mismatch (common next-themes pattern: don't render icon until mounted)

5. **Update src/app/layout.tsx:**
   - Wrap the body children with `<ThemeProvider>` inside the body tag
   - Keep `suppressHydrationWarning` on `<html>` element
   - Ensure Toaster (sonner) is still rendered
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Theme toggle component cycles through light/dark/system. ThemeProvider wraps the app in root layout. No hydration errors. Theme persists in localStorage via next-themes default behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Public navbar (sticky, responsive, hamburger) and admin sidebar layout (forced dark, Sheet on mobile)</name>
  <files>
    src/components/layout/public-navbar.tsx
    src/components/layout/mobile-nav.tsx
    src/components/layout/admin-sidebar.tsx
    src/app/admin/layout.tsx
    src/app/(public)/layout.tsx
  </files>
  <action>
1. Add Shadcn components needed: `npx shadcn@latest add sheet separator avatar dropdown-menu alert-dialog`

2. **src/components/layout/public-navbar.tsx** — Client component:
   - Sticky top navbar: `fixed top-0 w-full z-50 bg-background/80 backdrop-blur-sm border-b`
   - Left side: "AKM" or "Aung Kaung Myat" text/logo linking to `/`
   - Center/right: Navigation links as anchor links for future single-page sections:
     - About (#about), Projects (#projects), Experience (#experience), Education (#education), Skills (#skills), Contact (#contact)
   - Far right: ThemeToggle component
   - Desktop: all links visible in a row
   - Mobile (below md breakpoint): hide nav links, show hamburger icon button that triggers MobileNav

3. **src/components/layout/mobile-nav.tsx** — Client component:
   - Uses Shadcn Sheet component (side="right" or side="top" — Claude's discretion)
   - Triggered by hamburger menu icon button from PublicNavbar
   - Contains the same nav links as desktop (About, Projects, etc.) stacked vertically
   - ThemeToggle also included in the mobile sheet
   - Sheet closes when a link is clicked

4. **src/app/(public)/layout.tsx** — Route group layout for public pages:
   - Renders `<PublicNavbar />` at the top
   - Wraps children in `<main className="pt-16">` (offset for fixed navbar height)
   - Move the existing `src/app/page.tsx` into `src/app/(public)/page.tsx` so it uses the public layout
   - This route group does NOT affect the URL — pages are still at `/`, `/projects/[id]`, etc.

5. **src/components/layout/admin-sidebar.tsx** — Client component:
   - Sidebar nav with links: Dashboard (/admin), Projects (/admin/projects), Experience (/admin/experience), Education (/admin/education), Skills (/admin/skills), Messages (/admin/messages), Profile (/admin/profile)
   - Use lucide-react icons next to each link (LayoutDashboard, FolderKanban, Briefcase, GraduationCap, Wrench, MessageSquare, User)
   - Active link highlighting based on current pathname (use `usePathname()`)
   - Desktop: fixed left sidebar, width ~64 (w-64), full height
   - Mobile: hidden by default, triggered via a hamburger/menu button that opens a Shadcn Sheet (side="left")
   - Bottom of sidebar: LogoutButton component (from Plan 01)

6. **Update src/app/admin/layout.tsx** — Force dark mode for admin:
   - Wrap the admin layout in a `<div className="dark">` — this forces Tailwind dark mode classes regardless of the theme setting. This is the simplest approach and per user decision that admin is always dark.
   - Layout structure: AdminSidebar on the left, main content area on the right with padding
   - `<div className="dark bg-background text-foreground min-h-screen">` wrapping everything
   - Inside: flex layout with `<AdminSidebar />` and `<main className="flex-1 p-6 md:ml-64">` for content
   - Keep the auth guard (getUser check) from Plan 01
  </action>
  <verify>
    <automated>cd "/Users/aungkaungmyat/Desktop/personal projects" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Public navbar is sticky with section anchor links, hamburger menu on mobile with Sheet slide-out. Admin sidebar has navigation links with active state, forced dark mode wrapper, and Sheet on mobile. Both layouts build without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify theme toggle and navigation shell visually</name>
  <files>no files — visual verification only</files>
  <action>
Human visually verifies the theme system and navigation shell. Run `npm run dev` and check the following areas:
- Theme toggle behavior (three-state cycling)
- Public navbar (sticky, responsive, mobile hamburger)
- Admin sidebar (forced dark mode, mobile Sheet)
- Login page layout (centered card with branding)
  </action>
  <verify>
    1. Visit http://localhost:3000 — public navbar is sticky with "AKM" branding, section links (desktop), theme toggle icon top-right
    2. Click theme toggle — cycles Sun (light) to Moon (dark) to Monitor (system), page theme changes
    3. Refresh page — theme persists, no flash of wrong theme
    4. Resize to mobile — hamburger appears, opens Sheet with nav links
    5. Visit http://localhost:3000/login — centered card with "AKM Portfolio" branding, email/password form, GitHub OAuth button
    6. Visit http://localhost:3000/admin — redirects to /login if unauthenticated
    7. If Supabase configured: log in, verify admin has dark sidebar with nav links, Sheet on mobile
  </verify>
  <done>User confirms all visual elements match the design decisions: theme cycling works, navbar is responsive, admin is dark, login card is centered with branding.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero errors
2. Theme toggle cycles through light/dark/system with correct icons
3. Theme persists across page refresh with no flash
4. Public navbar is sticky, responsive, shows hamburger on mobile
5. Admin layout forces dark mode regardless of theme setting
6. Admin sidebar collapses to Sheet on mobile
7. All navigation links render correctly
</verification>

<success_criteria>
- Dark/light/system theme toggle works with icon cycling (Sun/Moon/Monitor) per user decision
- Theme persists in localStorage, no FOUC on load
- Admin is always dark mode per user decision
- Public navbar: sticky, section anchor links, hamburger on mobile per user decision
- Admin sidebar: left sidebar with content section links, Sheet on mobile per user decision
- Both layouts are responsive across mobile, tablet, and desktop
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
